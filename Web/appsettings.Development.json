{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // En desarrollo el key ring se guarda en una carpeta local del proyecto (no en el file server
  // de producción). Así las cookies sobreviven a reinicios locales sin depender del recurso de red.
  "DataProtection": {
    "KeysPath": "DataProtection-Keys"
  }
}
